server {
    listen 80;

    # Serve React app
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html =404;
    }

    # Serve the health check path
    location /healthz {
        return 200 'OK';  # Always returns a success for health check
    }
}
